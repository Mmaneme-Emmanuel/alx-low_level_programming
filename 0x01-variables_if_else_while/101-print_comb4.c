#include <stdio.h>

/**
*main - three digits counting
*Return: always 0 (success)
*/
int main(void)

{
int i;

	for (i = '0'; i <= 9; i++)

	putchar(i);

int j;
	for (j = '1'; j <= 9; j++);
	putchar(j);
int k;
	for (k = '2'; k <= 9; k++);
	putchar(k);

