#include "main.h"
/**
 * _islower - checker for lower character
 * @c: the character to be checked
 * Return: i if c is lower, 0 otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
